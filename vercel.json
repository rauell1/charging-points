{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push && next build",
  "functions": {
    "src/app/api/sync/rescan/route.ts": {
      "includeFiles": "upload/**"
    }
  },
  "crons": [
    {
      "path": "/api/sync/rescan",
      "schedule": "0 0 * * 0"
    }
  ]
}
